#!/usr/bin/env bash
set -euo pipefail

# Skip if cargo is not installed (e.g., for non-Rust developers)
if ! command -v cargo &> /dev/null; then
    echo "⚠ cargo not found - skipping Rust tests (install with: brew install rust)"
    exit 0
fi

cargo test --all-features
